# 报销自动化平台 · 功能手册

> 写给使用这个平台的人：社团成员、审核员，以及任何想了解它的人。
> 本文用大白话讲清楚每个界面**能做什么、不能做什么**，以及常见问题的答案。
> 想了解技术细节（怎么安装、怎么部署、怎么改代码），请看 `README.md` —— 那是给开发者看的，本文是给使用者看的。

---

## 1. 这个平台是干什么的

香港中文大学（深圳）的学生社团，举办活动花了钱，需要报销。这个平台把整个报销流程搬到了网上：

- 成员在网上**填报销单、传发票和凭证**
- 系统**自动识别发票**上的信息（购买方、金额、物品明细），帮你省去手抄
- 审核员在网上**看材料、写批注、通过或打回**
- 全程不用纸质单据，进度和反馈随时能查

---

## 2. 谁能用（身份规则）

| 身份 | 怎么获得 | 能做什么 |
|---|---|---|
| **社团成员** | 用 `@link.cuhk.edu.cn` 邮箱注册 | 提交报销申请、保存草稿、查看历史记录和审核反馈、被打回后修改重传 |
| **审核员** | 用 `@cuhk.edu.cn` 邮箱注册（注册即是审核员） | 查看所有成员的报销申请、在线看材料、写批注、通过或打回；可批准新审核员 |
| **双重身份** | 先用 `@link.cuhk.edu.cn` 注册，再申请成为审核员并被批准 | 登录后**自己选择**进入成员端还是审核员端 |

**规则细节：**

- 只能用 `@cuhk.edu.cn` 或 `@link.cuhk.edu.cn` 邮箱注册，其他邮箱一律被拒绝。
- 邮箱不能重复注册（注册过就不能再注册，也不需要再注册）。
- 密码没有强制复杂度要求，但**注册时至少 6 位**；建议自己设一个长密码，因为目前**没有"忘记密码"功能**，密码忘了只能联系管理员处理。
- 登录状态 **24 小时**后过期，需要重新登录。

---

## 3. 报销流程一览

### 成员视角

```
注册 / 登录
  → 选择报销类型（增值税 / 保险 / 路费 / 大量发票）
  → 第 1 步：上传发票和凭证（发票会自动识别）
  → 第 2 步：填写报销表
  → 第 3 步：确认提交
  → 等待审核
      ├─ 通过 → 完成 🎉（审核员端可下载存档）
      └─ 打回 → 首页出现红点 → 查看批注 → 重新编辑 → 再提交
```

### 审核员视角

```
登录 → 审核员工作台（看统计）
  → 材料审核（筛选 → 点开申请 → 看材料 → 写批注）
  → 通过 或 打回
  → 成员重传后，工作台出现"重审"提示 → 再审一轮
```

---

## 4. 页面说明

> 每页按固定格式介绍：**这是什么 / 能做什么 / 不能做什么**。
> 页面上带 `*` 号的字段是必填的。

### 4.1 首页（进入平台看到的第一个页面）

- **这是什么**：平台的门面，介绍平台是干什么的。
- **能做什么**：点击「登录 / 注册」进入登录页。
- **不能做什么**：这里没有其他入口；未登录时看不到任何功能页。

### 4.2 登录

- **这是什么**：输入邮箱和密码进入平台。
- **能做什么**：登录自己的账号；没有账号可点「立即注册」跳去注册。
- **不能做什么**：
  - 邮箱不存在会提示「邮箱未注册」；密码错误提示「密码错误」。
  - 无法在这里重置密码（平台暂无此功能）。
  - 登录后去哪取决于身份：普通成员 → 成员工作台；审核员 → 审核员工作台；双重身份 → 先选身份。

### 4.3 注册

- **这是什么**：创建账号的页面。
- **能做什么**：用校园邮箱注册。页面副标题写得很清楚：`@cuhk.edu.cn` 注册即**审核员**，`@link.cuhk.edu.cn` 注册是**社团成员**。
- **不能做什么**：
  - 不接受其他邮箱域名。
  - 两次输入的密码不一致会被拦下（提示「两次密码输入不一致」）。
  - 注册成功只是创建了账号，还需登录一次。

### 4.4 选择身份（仅双重身份可见）

- **这是什么**：既是成员又是审核员的人，登录后先在这里选今天要干什么。
- **能做什么**：点「社员登录」进成员端；点「审核员登录」进审核员端；也可以退出登录。
- **不能做什么**：选择身份不会改变你的权限——如果账号还没被批准为审核员，即使点「审核员登录」也会被送回登录页。

### 4.5 成员工作台（登录后默认首页）

- **这是什么**：成员的"总控制台"，所有入口都在这。
- **能做什么**：
  - 看三个数字：本月提交数、待审核数、已通过数。
  - 「增值税报销」大卡：直接开始增值税报销（点它直接进上传页）。
  - 「其他类报销」卡：选保险、路费、大量发票。
  - 「我的草稿」卡：打开草稿列表，**继续**上次没填完的单（系统按你填到第几步恢复进度），也可以删除不要的草稿。
  - 「查看历史提交」卡：跳转到**独立页面**（见 4.14），搜索、按日期筛选自己的历史提交，**下载**已提交的存档文件（ZIP）。
  - 「审核反馈」卡：**红色数字徽章 = 有新的审核结果没看**。点卡片进入**独立页面**（见 4.15），能看到每条申请是「已通过」还是「已打回」，打回的能看到审核员写的所有批注；最新一次被打回的申请可以点「重新编辑」修改后重传。
  - 「申请成为审核员」卡：弹出小窗，填邮箱和申请原因提交申请。
- **不能做什么**：
  - 不能看到别人的申请（历史、草稿、反馈都只显示你自己的）。
  - 不能在这里审核任何东西。
  - 「审核反馈」里的红点只代表**有更新**，不代表必须做什么；进入反馈页面后红点就会消失（**10 秒内**自动刷新，不用手动刷新页面）。

### 4.6 选择报销类型

- **这是什么**：进入非增值税报销时的类型选择页（保险 / 路费 / 大量发票）。
- **能做什么**：选一张卡片开始对应的报销流程。
- **不能做什么**：不能在这里同时提交多种类型的报销——一次提交只能是一种类型；如果这笔活动既有路费又有保险，需要**分开提交**。

### 4.7 上传材料（向导第 1 步）

- **这是什么**：报销流程第一步，传发票和各种凭证。
- **能做什么**：
  - 每种必需材料一张卡片，点击或拖拽文件即可上传，可一次选多个、可继续追加、每个文件能单独移除。
  - 发票上传后点「开始识别 N 张发票」，系统**自动识别**发票上的购买方、税号、开票日期、总额和物品明细，识别结果直接带进下一步的表单。
  - 识别失败或遇到纸质车票，可点「手动添加」自己填。
  - 顶部有步骤条（1 上传 → 2 填表 → 3 确认），可以知道走到哪了。
- **不能做什么**：
  - 每种材料有**格式、大小、张数**要求（见第 6 节速查表），超出上限会被拦截（提示「XX 最多上传 N 张」并只保留前 N 个）。
  - 必需材料没传齐（每种至少 1 份）时，「下一步」按钮是灰的。
  - 发票识别是"尽力而为"：识别不出或不准确时，可以在下一步**手动修改**，不要卡在这一步。
  - 这里只是上传，**还没有提交**；中途退出（返回首页）会问你是否存为草稿。

### 4.8 填写报销表（向导第 2 步）

- **这是什么**：填报销单的核心页面。系统已按发票识别结果帮你填好了大部分。
- **能做什么**：
  - **活动信息**：活动名称、学生组织名称、活动时间（最后一天）、报销时间。
  - **每张发票**：购买方（带有效性提示）、发票总额、报销明细表（物品名称、购买途径、单价、数量、小计自动算）、报销金额（不能超过该发票总额）、经手人。
  - 明细表可以「添加一行」、删除行（至少保留 1 行）；**路费报销的单价可以填负数**（如退票差价）。
  - **其他信息**：经办人、活动负责人意见（可不填）、支付宝账号。
  - 填完点「下一步：确认提交」，系统先自己检查一遍，有错会列出来。
- **不能做什么**：
  - **活动时间和报销时间不能晚于今天**。
  - 报销金额**超过发票总额**时输入框标红，"下一步"禁用，直到改对。
  - 支付宝账号必须填 **11 位手机号（1 开头、第 2 位 3–9）或邮箱格式**，否则不能继续。
  - 凡填写了物品名称的行，单价、数量、购买途径、是否可重复利用**必须齐全**，否则校验不通过。
  - 每张发票的**报销金额必须大于 0**、经手人必须填写。
  - 页面上有两个"合计"容易混淆：每张发票明细表里的小计合计（明细行金额总和），和页面底部的实际花费总计（所有发票**报销金额**之和）。提交以**报销金额**为准。

### 4.9 确认提交（向导第 3 步）

- **这是什么**：提交前的最后检查页，所有内容**只读预览**。
- **能做什么**：
  - 核对活动信息、每张发票、合计金额、经办人、材料清单。
  - 「预览 Excel 报销表」：**在浏览器内直接预览**报销表生成效果（全屏显示），确认无误后可在预览窗口里下载 Excel 文件；提交前先看看长什么样。
  - 「保存草稿」：想晚点再交，先存起来。
  - 「确认提交」：金色大按钮，提交后系统会：核对表单 → 保存材料 → 在发票上拼接报销信息 → 生成 Excel → 打包成 ZIP 存档 → 入库。
  - 提交成功后显示存档文件名，可点「发起新的报销申请」再来一单。
- **不能做什么**：
  - 这里**不能修改内容**，要改只能点「上一步」回去改。
  - 提交按钮点了就真的交了，没有"撤销"功能——交完后只能等审核结果。
  - 校验没通过时会被拒绝提交，并逐条列出原因（红字面板）。

### 4.10 被打回后的重新编辑

- **这是什么**：不是单独页面，而是被打回的申请重新进入向导时的工作模式。
- **能做什么**：
  - 进入上传页时顶部有紫色横幅：「正在重新编辑被打回的报销申请，原有文件已保留，可替换或追加」。
  - 每张发票的错误，按审核员批注逐项改掉（发票、凭证、表单、材料的批注都会展示在反馈弹窗里）。
  - 如果批注涉及材料，回到第 1 步重新传；只涉及表单内容就直接进第 2 步。
  - 提交后该申请进入「重审」队列，审核员会再审一轮。
- **不能做什么**：
  - 原有文件不会自动清掉——**新增发票会替换原发票**（需要重新识别），其他材料是"新文件和旧文件并存"；如果不想要旧文件，记得手动移除。
  - 重传前旧的提交记录和批注仍保留在系统里（审核员端可见），只是成员端不再显示旧版本。

### 4.11 审核员工作台

- **这是什么**：审核员的"总控制台"。
- **能做什么**：
  - 四个统计数字：待审核（橙）、已通过（绿）、已打回（红）、**已打回材料重审**（紫，>0 时带红点提醒，说明有成员重传等你再审）。
  - 「材料审核」大卡：进入审核列表。
  - 「给予权限」卡：处理"申请成为审核员"的申请。
  - 重审列表直接展示最新的 5 条重审申请，点一下直达审核。
- **不能做什么**：
  - 看不到普通成员的工作台和草稿；审核员端和成员端是分开的。

### 4.12 材料审核（列表 + 审核弹窗）

- **这是什么**：审核员干活的地方。
- **能做什么**：
  - **筛选**：按状态（待审核/已通过/已打回/重审）、报销类型、起止日期、**社团**（输入时自动补全名单）筛选；点「**恢复默认**」一键清空全部筛选条件。
  - **列表**：标题显示为「**社团名-日期-报销申请**」（更易辨认，无社团名的老申请仍显示文件名），旁边有类型徽章、状态徽章和**报销总金额**（¥xx.xx）。
  - 点击申请打开**审核弹窗**（可切全屏，Esc 退出）：材料清单 → 点材料看缩略图 → 点缩略图放大查看（支持上一张/下一张、PDF 在线预览/新窗口打开）。
  - 点「报销表」直接在浏览器内**预览**报销表效果（可全屏，全屏内也能写批注），不再需要下载后打开。
  - 每份材料和报销表都有**批注框**，内置常用批注模板（如"发票缺少公章""凭证无法证明活动真实性""组织名称有误"等），点模板自动填入、可继续编辑。
  - 底部操作栏（仅"待审核"和"重审"状态显示）：「确认无误」通过 /「打回修改」打回（打回必须写批注，成员靠批注知道改什么）。
  - 已通过的申请可以点「下载 ZIP」下载存档。
- **不能做什么**：
  - 已通过或已打回的申请，操作栏不显示——**不能重复审核**同一份申请。
  - 不写批注就关闭弹窗会弹确认框（防止批注白写）。
  - 审核员能看到**所有人的申请**（含历史版本），这是设计如此——审核是全局的，不是"只看自己审过的"。

### 4.13 给予权限（审核员申请管理）

- **这是什么**：处理"申请成为审核员"的申请列表。
- **能做什么**：查看申请人邮箱、申请原因、申请时间；点「通过」批准（批准后该账号立即获得审核员权限，下次登录就能选审核员身份）。
- **不能做什么**：
  - 当前版本**只有"通过"按钮，没有"拒绝"**——不打算批准的申请只能放着不处理（会一直挂在列表里）。
  - 不能撤销已批准的审核员权限。

### 4.14 查看历史提交（成员端独立页面）

- **这是什么**：成员查看自己所有提交记录的独立页面（从首页「查看历史提交」卡片进入）。
- **能做什么**：
  - **搜索**：按标题（社团名-活动名-日期-报销申请）或原始文件名搜；**时间筛选**：本月/近三个月/今年/全部。
  - 每条显示：标题、大小、提交时间、**状态徽章**（已通过/已打回/重审/待审核）、报销类型徽章。
  - 已提交的申请可以点「下载」拿到存档 ZIP（已通过的也能下载）。
- **不能做什么**：
  - 只显示你自己的申请；被打回后重传过的申请，这里只显示最新版本。

### 4.15 审核反馈（成员端独立页面）

- **这是什么**：审核员与管理员对报销申请的处理结果和批注（从首页「审核反馈」卡片进入，**进入即视为已读**，首页红点消失）。
- **能做什么**：
  - 每条显示「已通过」或「已打回」徽章；打回的能看到全部批注：发票、凭证、材料、报销表四类，管理员批注会带紫色标识。
  - 「重新编辑」：带着原有表单内容和材料回到向导，按批注修改后重传（进入「重审」队列）。
  - 「意见反馈」：对处理结果有异议时，跳转到申诉页面提交意见。
- **不能做什么**：
  - 待审核（还没结果）的申请不显示在这里。
  - 重新编辑时原有文件不会自动清掉——新增发票会替换原发票（需重新识别），其他材料"新旧并存"，不要的旧文件要手动移除。

### 4.16 历史审核（审核员端）

- **这是什么**：审核员查看**所有已处理的申请**历史记录的页面（从审核员工作台「历史审核」卡片进入）。
- **能做什么**：
  - 筛选与材料审核页一致：状态（已通过/已打回）、报销类型、起止日期、**社团**（自动补全），可「恢复默认」。
  - 列表显示标题（社团名-日期-报销申请）、状态徽章、类型徽章、时间、审核人；已通过的可以「下载 ZIP」存档。
  - 点开申请可以查看材料和报销表（报销表可在浏览器内预览）。
- **不能做什么**：
  - 历史申请**不能再次审核**（没有通过/打回按钮），只能查看和下载。

---

## 5. 四种报销类型

| 类型 | 适合的场景 | 需要上传的材料 | 特别注意 |
|---|---|---|---|
| **增值税报销** | 常规活动开销（采购物资、场地等） | 发票 + 活动凭证 | 凭证必须是**活动现场照片**；发票是**增值税普通发票** |
| **保险报销** | 为活动购买保险 | 发票 + 保险保单 | 保单支持 PDF（加密的电子保单系统会自动尝试解密） |
| **路费报销** | 出行产生的交通费用 | 交通票据 + 乘车人员身份凭证 + 出行行程单 + 出行支付记录 | 票据上的数量要和表单里填的票据区块**一一对应**；单价可填负数（退票差价）；电子发票自动识别，纸质车票手动填 |
| **大量发票报销** | 一张单子有大量发票 | 发票 + 活动凭证 | 发票**最多 30 张** |

> 温馨提示：一次提交只能是一种类型。混合开销请拆成多张单。

---

## 6. 材料要求速查

| 材料 | 允许格式 | 单张最大 | 张数要求 |
|---|---|---|---|
| 发票 | PDF / PNG / JPG | 10 MB | 至少 1 张（大量发票报销最多 30 张） |
| 活动凭证 | 仅 PNG / JPG | 20 MB | 1–20 张 |
| 保险保单 | PDF / PNG / JPG | 20 MB | 1–10 张 |
| 乘车人员身份凭证 | PDF / PNG / JPG | 10 MB | 1–50 张（每人一张） |
| 出行行程单 | PDF / PNG / JPG | 20 MB | 1–10 张 |
| 出行支付记录 | 仅 PNG / JPG | 10 MB | 1–20 张 |

---

## 7. 常见问题 FAQ

**Q：注册时提示邮箱不支持？**
只能使用 `@cuhk.edu.cn`（审核员）或 `@link.cuhk.edu.cn`（成员）邮箱，其他域名会被拒绝。

**Q：发票传上去识别不出来怎么办？**
识别是"尽力而为"。传了图片/PDF 后点「开始识别」，失败或结果不准时：点击「手动添加」自己填，或在第 2 步直接修改识别出的内容。纸质车票不支持识别，请用手动方式。

**Q：提交时提示"表单校验未通过"？**
页面上会逐条列出具体原因（红色面板），常见的有：报销金额超过发票总额、必填项没填、明细行信息不完整、支付宝账号格式不对、时间晚于今天。按提示改掉即可。

**Q：填到一半想离开，会丢吗？**
在向导页（上传/填表/确认）点「返回首页」时，系统会问你是否保存草稿；也可以在任何一步直接点「保存草稿」。下次在首页「我的草稿」里点条目，会从你停下的步骤继续。

**Q：提交后多久能审完？**
取决于审核员。审核员操作后，你的首页红点**最多 10 秒内**自动出现，不用手动刷新页面。

**Q：被打回了，要全部重填吗？**
不用。点「重新编辑」会带着**原有的表单内容和材料**进入向导，你只需要按批注改掉有问题的地方。注意：新增发票会替换原发票并需重新识别。

**Q：审核员能看到哪些申请？**
所有成员的申请（含历史版本），审核是全局的。成员端则只能看到自己的。

**Q：申请成为审核员后多久生效？**
等审核员在「给予权限」页点「通过」。批准后登录时即可选择审核员身份。（当前版本无法"拒绝"申请，只能放着不处理。）

**Q：能下载自己提交过的存档吗？**
能。首页「查看历史提交」里每一条都有「下载」按钮。

**Q：发票上传后发现系统在发票上加了字？**
这是自动的：提交时系统会在发票图片上拼接报销信息（组织、活动、物品、金额），方便审核对照。这是正常功能，不需要你操作。

**Q：系统识别发票时会核对哪些信息？**
购买方名称应为「香港中文大学（深圳）」、税号 `12440300066312613F`。识别结果与预期不符会在发票卡片上显示警告标记，但不会强制拦截——请自行确认发票是否开票正确。

---

## 8. 注意事项

- **只传真实材料**：系统对发票和活动凭证的文件格式/大小检查比较宽松（历史原因），请自觉只上传真实、清晰、符合规定的材料，否则会被审核员打回。
- **账号安全**：登录状态 24 小时过期；请勿把账号借给别人。
- **发票类型**：目前识别针对**增值税普通发票**；其他发票类型识别效果不保证。
- **本月 OCR 免费额度**：发票识别依赖百度云的免费额度（2000 次/月），用完需要等待次月恢复或联系管理员配置。

---

*本手册基于 2026-08-24 的版本编写；平台功能更新后如需刷新，找开发者重新生成。*
